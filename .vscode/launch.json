{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "playdate",
            "request": "launch",
            "name": "Build and Run (Simulator)",
            "preLaunchTask": "Build and Run (Simulator)"
        },
        {
            "type": "node",
            "request": "launch",
            "name": "Launch-mac",
            "program": "${workspaceFolder}/.vscode/run.js",
            "preLaunchTask": "build",
            "internalConsoleOptions": "neverOpen",
            "env": { "PLAYDATE_OUTPUT" : "${config:playdate.output}" }
          }
    ]
}